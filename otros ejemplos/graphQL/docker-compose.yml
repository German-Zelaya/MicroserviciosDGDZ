version: '3.8'

services:
  order-service:
    build: .
    container_name: order-service-graphql
    ports:
      - "8082:8082"
    networks:
      - order-network

networks:
  order-network:
    driver: bridge